# BNPL Figma Flow Map — node 814:23900

Source: https://www.figma.com/design/geEFxJ11n2KySAZB6zsjEh/BNPL?node-id=814-23900

## Board sections discovered from Figma metadata

1. Homepage — active-account BNPL home
   - 1885:12069 `BNPL Home` -> route `/checkout/app-home`

2. Next Up / dues selection
   - 814:24007 `Intro - 1 Time show` -> route `/checkout/next-up`
   - 1843:17915 `My dues` -> route `/checkout/dues`
   - 1907:11055 `My dues` with sheet -> represented by payment-method routes, not a permanent overlay on Dues

3. Insights
   - 1579:11144 `Insights · Full history` -> route `/checkout/insights`
   - 1579:11218 `Insights · Ramping up` -> pending variant
   - 1579:11321 `Statement · Past month selected (Feb)` -> pending variant
   - 1579:11292 `Insights Empty` -> route alias added `/checkout/insights/empty`
   - 1579:10592 `Insights - Category` -> route alias added `/checkout/insights/category`

4. My Purchases
   - 814:24392 `My purchases` -> route `/checkout/purchases`
   - 894:8481 `My purchases` variant -> pending exact variant

5. Purchase Plan Clicked
   - 814:24413 / 845:8283 detail bottom plan frames -> route `/checkout/detail`

6. Payment Method
   - 814:24082 `Payment Method` -> route `/checkout/payment-method`
   - 816:47301 `Payment Method Selected` -> route `/checkout/payment-method/selected`
   - 816:46400 `Add New` -> route `/checkout/payment-method/add-card`
   - 835:17761 `Added New` -> route `/checkout/payment-method/added`

7. OTP
   - 814:24658 `iPhone 16 - 26` -> route `/checkout/otp`

8. Payment Status
   - 814:24673 `Processing` -> route `/checkout/processing`
   - 814:24685 `Insufficient Funds` -> route `/checkout/insufficient`
   - 814:24703 `Declined` -> route `/checkout/declined`
   - 814:24721 `Success` -> route `/checkout/success`

## Immediate UI/UX repair focus

- Use real SAR/Riyal SVG assets (`riyalDark.svg`, `riyalOnPrimary.svg`) instead of Arabic Riyal glyph text.
- Back/close controls use Figma SVG assets in round 50px controls.
- Preserve Dues base screen; payment method is a separate route/sheet flow and should not stale-overlay the Dues route.
- Home must use Figma decorative home elements, real action icons, merchant logos, and 402px iPhone frame sizing.

## Remaining fidelity gaps after first repair slice

- Insights variants need exact separate content states.
- Purchase-detail bottom sheet variants need exact per-node geometry.
- Payment-method sheet uses route-complete approximation; asset-specific card/Apple/Google icons still need extraction if client-presentable.
